# Use Set/Map for O(1) Lookups
Source: https://antigravity.codes/agent-skills/js/js-set-map-lookups

## AI Worker Instructions
When the user requests functionality related to Use Set/Map for O(1) Lookups, follow these guidelines and utilize this context.

## Scraped Content

# Use Set/Map for O(1) Lookups
```
const allowedIds = ['a', 'b', 'c', ...]items.filter(item => allowedIds.includes(item.id))
```
```
const allowedIds = ['a', 'b', 'c', ...]items.filter(item => allowedIds.includes(item.id))
```
```
const allowedIds = new Set(['a', 'b', 'c', ...])items.filter(item => allowedIds.has(item.id))
```
```
const allowedIds = new Set(['a', 'b', 'c', ...])items.filter(item => allowedIds.has(item.id))
```

