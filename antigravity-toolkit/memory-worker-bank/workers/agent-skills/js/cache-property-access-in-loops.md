# Cache Property Access in Loops
Source: https://antigravity.codes/agent-skills/js/js-cache-property-access

## AI Worker Instructions
When the user requests functionality related to Cache Property Access in Loops, follow these guidelines and utilize this context.

## Scraped Content

# Cache Property Access in Loops
```
for (let i = 0; i < arr.length; i++) {  process(obj.config.settings.value)}
```
```
for (let i = 0; i < arr.length; i++) {  process(obj.config.settings.value)}
```
```
const value = obj.config.settings.valueconst len = arr.lengthfor (let i = 0; i < len; i++) {  process(value)}
```
```
const value = obj.config.settings.valueconst len = arr.lengthfor (let i = 0; i < len; i++) {  process(value)}
```

