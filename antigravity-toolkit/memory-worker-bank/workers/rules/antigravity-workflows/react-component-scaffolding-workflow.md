# React Component Scaffolding Workflow
Source: https://antigravity.codes/rules/antigravity-workflows/react-component-scaffolding-workflow

## AI Worker Instructions
When the user requests functionality related to React Component Scaffolding Workflow, follow these guidelines and utilize this context.

## Scraped Content

# React Component Scaffolding Workflow
```
---description: Create a new React component with standard structure and styling---1. Ask the user for the name of the new component (e.g., "Button", "Header").2. Create a new directory for the component in src/components/[ComponentName].3. Create the main component file src/components/[ComponentName]/index.jsx.4. Add the following boilerplate code to index.jsx:
```
```
---description: Create a new React component with standard structure and styling---1. Ask the user for the name of the new component (e.g., "Button", "Header").2. Create a new directory for the component in src/components/[ComponentName].3. Create the main component file src/components/[ComponentName]/index.jsx.4. Add the following boilerplate code to index.jsx:
```
```
src/components/[ComponentName]
```
```
src/components/[ComponentName]/index.jsx
```
```
index.jsx
```
```
5. Create a CSS file src/components/[ComponentName]/styles.css.6. Add a basic style rule for the container class.7. Verify that the component is exported correctly.
```
```
5. Create a CSS file src/components/[ComponentName]/styles.css.6. Add a basic style rule for the container class.7. Verify that the component is exported correctly.
```
```
src/components/[ComponentName]/styles.css
```

# Related Rules

# React Hook Generator Workflow

# Antigravity Workflow Fundamentals

# Git Feature Branch Workflow

# Recommended Workflows

# React Performance Profiling

# Debug Memory Leaks in React

# Scaffold New Component

# Recommended MCP Servers

# 21st.dev Magic
Create crafted UI components inspired by the best 21st.dev design engineers.

# Context Templates
An open-source collection of reusable context templates designed to assist developers in structuring prompts, configurations, and workflows across various development tasks. Community contributions are encouraged to expand and refine available templates.

# DevExpress
Documentation MCP server — Get instant, AI-powered access to 300,000+ help topics on [DevExpress](https://www.devexpress.com) UI Component APIs — right in the AI Coding Assistant/IDE of your choice.

# Take It Further
Maximize your productivity with these powerful resources
Combine this rule with automated workflows for maximum efficiency and consistency.
Discover advanced techniques for creating custom rules and mastering Antigravity.

