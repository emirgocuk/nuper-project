# Dependency Reset Workflow
Source: https://antigravity.codes/rules/antigravity-workflows/dependency-reset-workflow

## AI Worker Instructions
When the user requests functionality related to Dependency Reset Workflow, follow these guidelines and utilize this context.

## Scraped Content

# Dependency Reset Workflow
```
---description: Completely reset node_modules and reinstall dependencies to fix issues---1. Delete the existing node_modules folder to ensure a clean slate.// turbo2. Run rm -rf node_modules3. Delete the package-lock.json file to avoid version conflicts.// turbo4. Run rm package-lock.json5. Reinstall all dependencies from scratch.// turbo6. Run npm install
```
```
---description: Completely reset node_modules and reinstall dependencies to fix issues---1. Delete the existing node_modules folder to ensure a clean slate.// turbo2. Run rm -rf node_modules3. Delete the package-lock.json file to avoid version conflicts.// turbo4. Run rm package-lock.json5. Reinstall all dependencies from scratch.// turbo6. Run npm install
```
```
rm -rf node_modules
```
```
rm package-lock.json
```
```
npm install
```
```
npm cache clean --force
```
```
rm -rf node_modules yarn.lock && yarn install
```

# Related Rules

# Antigravity Workflow Fundamentals

# Git Feature Branch Workflow

# React Component Scaffolding Workflow

# Recommended Workflows

# Debug Module Not Found Errors

# Nuke & Reinstall

# Debug 'Module Not Found' After Git Pull

# Recommended MCP Servers

# Context Templates
An open-source collection of reusable context templates designed to assist developers in structuring prompts, configurations, and workflows across various development tasks. Community contributions are encouraged to expand and refine available templates.

# Drata
Get hands-on with our experimental MCP server—bringing real-time compliance intelligence into your AI workflows.

# Hiveflow
Create, manage, and execute agentic AI workflows directly from your assistant.

# Take It Further
Maximize your productivity with these powerful resources
Combine this rule with automated workflows for maximum efficiency and consistency.
Discover advanced techniques for creating custom rules and mastering Antigravity.

