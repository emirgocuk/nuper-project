# Fix 'Too Many Re-renders' Error
Source: https://antigravity.codes/workflows/qa-debugging/fix-react-too-many-re-renders-error

## AI Worker Instructions
When the user requests functionality related to Fix 'Too Many Re-renders' Error, follow these guidelines and utilize this context.

## Scraped Content

# Fix 'Too Many Re-renders' Error
```
setCount(count + 1)
```
```
useEffect
```
```
const fetchData = useCallback(() => {     return { data: 'value' };   }, []);
```
```
const fetchData = useCallback(() => {     return { data: 'value' };   }, []);
```
```
onClick={handleClick()}
```
```
onClick={handleClick}
```

# How to Use This Workflow
- Click "Download" above
- In your project, create the directory: .agent/workflows/
```
.agent/workflows/
```
- Save the file as fix-react-too-many-re-renders-error.md
```
fix-react-too-many-re-renders-error.md
```
- In Antigravity, type /fix_react_too_many_re_renders_error or just describe what you want to do
```
/fix_react_too_many_re_renders_error
```
Learn more about workflows →

# Related Workflows

# React Performance Profiling

# Debug Memory Leaks in React

# Debug Slow API Routes (Performance Profiling)

